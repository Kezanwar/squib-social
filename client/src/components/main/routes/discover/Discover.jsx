import React from 'react'
import RouteWrapper from '../../../layout/RouteWrapper'
import Loading from '../../Loading'

const Discover = () => {
  return (
    <RouteWrapper id={'discover'} className={'discover'}>
      <Loading />
    </RouteWrapper>
  )
}

export default Discover
